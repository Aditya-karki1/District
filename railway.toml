[build]
builder = "nixpacks"
buildCommand = "npm install --prefix district && npm run build --prefix district && npm install --prefix server"

[deploy]
startCommand = "node server/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.variables]
NODE_ENV = "production"
